import DriversCleanersTermsPage from "@/modules/root/drivers-cleaners-terms";

export const metadata = {
  title: "Terms & Condition (Drivers/Cleaners) - MovMonkey",
};

export default function DriversCleanersTerms() {
  return <DriversCleanersTermsPage />;
}
