import DriverMessagePage from "@/modules/driver/message";

export const metadata = {
  title: " Driver Messages- MovMonkey",
};

export default function CreatedJob() {
  return <DriverMessagePage />;
}
